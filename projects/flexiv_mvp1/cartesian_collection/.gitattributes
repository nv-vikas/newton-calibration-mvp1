# These files participate in byte-level provenance hashes. Preserve exact bytes.
* -text
# Python's CSV writer emits standard CRLF records; this is not trailing data.
proposal/commands/*.csv whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
